import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Emlak CRM - Emlakçılar için CRM Sistemi",
  description: "Emlakçılar için geliştirilen CRM + otomasyon + çoklu platform paylaşım sistemi",
  keywords: ["emlak", "crm", "real estate", "property management", "customer management"],
  authors: [{ name: "Emlak CRM Team" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="tr" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
